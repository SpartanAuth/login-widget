# Basic App Example

## Installation

Run the following command to install dependencies:

```
npm install
```

## Running the Example

After installation, start the example with:

```
npm run start
```
`