import './components/login-widget';
import './components/auth-settings';
// @deprecated — spartan-invite is kept for backwards compatibility.
// Customers should point invite email redirect URLs at the spartan-login page instead.
import './components/invite-widget';
